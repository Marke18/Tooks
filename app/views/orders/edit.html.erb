<%= form_with(model: @order, local: true) do |form| %>

<% end %>

<div class="new">
  <h1>Modifica l'ordine </h1>

  <%= render 'form' %>
</div>

<div class="link"><strong>
  <%= link_to 'Ordini', orders_path  %>
  </strong>
</div>
